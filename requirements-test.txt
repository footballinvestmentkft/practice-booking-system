# Testing dependencies for CI/CD pipeline
pytest==9.0.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
pytest-xdist==3.8.0  # Parallel testing support for Integration Critical Suite
httpx==0.25.2
pytest-mock==3.12.0
# HTTP client for E2E tests
requests==2.32.5

# Performance testing
locust==2.17.0

# Security testing
safety==2.3.5
bandit==1.7.5

# Playwright E2E (used by e2e-fast-suite and test_tournament_monitor_e2e)
# 1.44+ required for Ubuntu 24.04 (Noble) libasound2t64 package name support
playwright==1.49.0
pytest-playwright==0.5.0

# Mutation testing — measures real test effectiveness (kill rate per module)
# Pin to 2.x: mutmut 3.x has a subprocess config-loading bug with this project's pytest setup
mutmut==2.4.4
name: Test Baseline Check
# Repo transferred 2026-06-26: football-investment → footballinvestmentkft

on:
  pull_request:
    branches: [ main, develop, "feature/*", "feat/*", "fix/*", "refactor/*" ]
  push:
    branches: [ main, develop ]
  workflow_dispatch:  # manual trigger via GitHub UI → Actions → Run workflow

# Concurrency control: Only 1 baseline pipeline per branch
# Prevents duplicate runs, ensures deterministic CI state
concurrency:
  group: ci-baseline-${{ github.ref }}
  cancel-in-progress: true

jobs:
  unit-tests:
    name: "Unit Tests (Baseline: 0 failed, 0 errors)"
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: lfa_intern_system_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install -r requirements-test.txt
        pip install pytest pytest-mock
        pip check

    - name: Run DB migrations
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: alembic upgrade head

    - name: Run unit + integration tests with coverage
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        pytest tests/unit/ tests/database/ tests/integration/tournament/ tests/integration/web_flows/ tests/integration/domain/ tests/biometric/ \
          -q --tb=line -v \
          --cov=app \
          --cov-branch \
          --cov-report=term-missing \
          --cov-fail-under=85 \
          --durations=20

    - name: Run migration rollback tests (DDL round-trip)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        # These tests perform actual downgrade/upgrade DDL — run separately
        # after the main suite. The autouse fixture always restores DB to head,
        # so subsequent steps (api_smoke, coverage) see a clean schema.
        pytest tests/integration/test_migration_rollback.py -v --tb=short

    - name: Run api_smoke tests (coverage append)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        pytest tests/integration/api_smoke/ -q --tb=line \
          --cov=app \
          --cov-branch \
          --cov-append

    - name: Enforce coverage thresholds (stmt >= 88%, branch >= 80%, combined >= 85%)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        python -m coverage xml -o coverage.xml
        python -m coverage report --show-missing --fail-under=85
        python .github/scripts/check_coverage.py

    - name: Upload coverage report
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: coverage-report-combined
        path: coverage.xml
        retention-days: 30

    - name: Parse test results
      id: test-results
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        RESULTS=$(pytest tests/unit/ -q --tb=no \
          2>&1 | tail -1)
        echo "results=$RESULTS" >> $GITHUB_OUTPUT

        # Check for failures or errors
        if echo "$RESULTS" | grep -qE "[0-9]+ failed"; then
          echo "❌ Tests FAILED - merge blocked"
          exit 1
        fi
        if echo "$RESULTS" | grep -qE "[0-9]+ error"; then
          echo "❌ Tests have ERRORS - merge blocked"
          exit 1
        fi

        echo "✅ All tests passed - baseline maintained"

    - name: Baseline validation
      if: failure()
      run: |
        echo "::error::Test baseline violated. Required: 0 failed, 0 errors"
        echo "Current test suite must maintain 100% pass rate."
        echo "See test-infra-stable-baseline tag for reference."
        exit 1

  api-tests:
    name: "API Tests (Baseline: 0 failed, 0 errors)"
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: lfa_intern_system_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install -r requirements-test.txt
        pip install pytest pytest-mock
        pip check

    - name: Run DB migrations
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: alembic upgrade head

    - name: Verify test collection
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        COUNT=$(pytest tests/api/ --collect-only -q 2>&1 | grep -E "^[0-9]+ test" | grep -oE "^[0-9]+")
        echo "Collected $COUNT tests in tests/api/"
        if [ -z "$COUNT" ] || [ "$COUNT" -lt 1 ]; then
          echo "::error::No tests collected from tests/api/ — suite may be empty or broken"
          exit 1
        fi

    - name: Run API tests
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        pytest tests/api/ -v --tb=short --maxfail=0

    - name: API baseline validation
      if: failure()
      run: |
        echo "::error::API test baseline violated. Required: 0 failed, 0 errors"
        exit 1

  api-module-integrity:
    # Verifies that all tournament endpoint modules import cleanly and the
    # route registry is intact after every refactoring split.
    # No database required — pure Python import + router introspection.
    # Add new modules here whenever a split is done.
    name: "API Module Integrity (import + route count)"
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Verify tournament endpoint modules import cleanly
      env:
        # Fake URL — create_engine() validates format only, no connection at import time
        DATABASE_URL: postgresql://ci:ci@localhost:5432/ci_check
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        python -c "
        import sys

        # ── lifecycle split (2026-02-18) ──────────────────────────────────────
        from app.api.api_v1.endpoints.tournaments.lifecycle import router, record_status_change
        print('✅ lifecycle: router + record_status_change')

        from app.api.api_v1.endpoints.tournaments.lifecycle_instructor import router as r_instructor
        print('✅ lifecycle_instructor: router')

        from app.api.api_v1.endpoints.tournaments.lifecycle_updates import router as r_updates
        print('✅ lifecycle_updates: router')

        # ── generator split (2026-02-18) ──────────────────────────────────────
        from app.api.api_v1.endpoints.tournaments.ops_scenario import router as r_ops
        from app.api.api_v1.endpoints.tournaments.ops_scenario import _simulate_tournament_results
        print('✅ ops_scenario: router + _simulate_tournament_results')

        from app.api.api_v1.endpoints.tournaments.generator import router as r_gen
        print('✅ generator: router')

        # ── record_status_change external-import compat ───────────────────────
        # rewards.py and rewards_v2.py import this; must stay importable from lifecycle
        from app.api.api_v1.endpoints.tournaments.lifecycle import record_status_change as rsc
        assert callable(rsc), 'record_status_change must be callable'
        print('✅ record_status_change importable from lifecycle (rewards compat)')

        # ── route count guard ──────────────────────────────────────────────────
        from app.api.api_v1.endpoints.tournaments import router as main_router
        routes = [r.path for r in main_router.routes]
        route_count = len(routes)
        EXPECTED_MIN = 71  # Bump this whenever new routes are added
        assert route_count >= EXPECTED_MIN, (
            f'Route count {route_count} < expected minimum {EXPECTED_MIN}. '
            'A router include_router() call may be missing in __init__.py.'
        )
        print(f'✅ Route count: {route_count} (minimum: {EXPECTED_MIN})')

        # ── lifecycle split route presence ─────────────────────────────────────
        required_routes = [
            '/{tournament_id}/assign-instructor',
            '/{tournament_id}/instructor/accept',
            '/{tournament_id}/instructor/decline',
        ]
        for path in required_routes:
            assert path in routes, f'Missing route: {path}'
        print(f'✅ All {len(required_routes)} lifecycle_instructor routes present')

        # ── ops route presence ─────────────────────────────────────────────────
        assert '/ops/run-scenario' in routes, 'Missing route: /ops/run-scenario'
        print('✅ /ops/run-scenario route present')

        print()
        print('✅ API module integrity: ALL CHECKS PASSED')
        "

    - name: Integrity check failure annotation
      if: failure()
      run: |
        echo "::error::API module integrity check failed."
        echo "A recent refactoring split likely broke an import or forgot to register a router."
        echo "Run locally: python -c \"from app.api.api_v1.endpoints.tournaments import router\""
        exit 1

  hardcoded-id-guard:
    # Prevents hardcoded foreign key IDs from being reintroduced into unit tests.
    # This ensures tests remain isolated and don't rely on seed data.
    # See TESTING_GUIDELINES.md for details.
    name: "Hardcoded FK ID Guard (lint)"
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Check for critical hardcoded FK patterns
      run: |
        echo "Checking for critical hardcoded FK patterns (user_id=1, user_license_id=1)..."
        VIOLATIONS=0

        # Check for user_id=1 (most common problem pattern we removed)
        # Exclude: concurrency tests, mock tests, API client tests (legitimate uses)
        if grep -rn "user_id=1[^0-9]" tests/unit/services/ --include="*.py" 2>/dev/null | grep -v "__pycache__"; then
          echo "❌ ERROR: user_id=1 found in service tests"
          VIOLATIONS=1
        fi

        if grep -rn "user_id=1[^0-9]" tests/unit/tournament/ --include="*.py" 2>/dev/null | \
           grep -v "concurrency" | grep -v "checkin" | grep -v "__pycache__"; then
          echo "❌ ERROR: user_id=1 found in tournament tests"
          VIOLATIONS=1
        fi

        # Check for user_license_id=1 (another critical pattern we removed)
        if grep -rn "user_license_id=1[^0-9]" tests/unit/ --include="*.py" 2>/dev/null | \
           grep -v "concurrency" | grep -v "__pycache__"; then
          echo "❌ ERROR: user_license_id=1 found in tests"
          VIOLATIONS=1
        fi

        if [ $VIOLATIONS -eq 1 ]; then
          echo ""
          echo "Tests must use factory fixtures instead of hardcoded IDs"
          echo "See TESTING_GUIDELINES.md for migration guide"
          exit 1
        fi

        echo "✅ No critical hardcoded FK patterns (user_id=1, user_license_id=1) found"

    - name: Guard check summary
      run: |
        echo "✅ All hardcoded FK ID checks passed"
        echo "Unit tests remain isolated with no seed dependencies"

  credit-audit:
    # Static AST scan: every function that mutates .credit_balance must also
    # create a CreditTransaction.  No DB required — runs on source code only.
    name: "Credit Balance Audit (static AST)"
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Scan for credit_balance mutations without CreditTransaction
      run: |
        echo "Scanning app/ for unlogged credit_balance mutations..."
        python scripts/audit_credit_transactions.py --exit-code

  smoke-tests:
    name: E2E Smoke Tests
    runs-on: ubuntu-latest
    needs: unit-tests

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: lfa_intern_system_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install -r requirements-test.txt
        pip install pytest pytest-playwright
        playwright install chromium
        pip check

    - name: Run DB migrations
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: alembic upgrade head

    - name: Seed tournament types
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: python scripts/seed_tournament_types.py

    - name: Seed LFA test users (@lfa-seed.hu)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: python scripts/seed_lfa_test_users.py

    - name: Run E2E smoke tests
      run: |
        echo "⚠️  Smoke tests SKIPPED - All smoke tests are Playwright/Streamlit UI tests"
        echo "Playwright tests require headed browser and don't work in GitHub Actions headless CI"
        echo "Covered by:"
        echo "  - tests/e2e/test_champion_badge_regression.py (Playwright)"
        echo "  - tests/e2e/test_instructor_dashboard_tab_smoke.py (Playwright)"
        echo "  - tests/e2e/test_tournament_manager_sidebar_nav.py (Playwright)"
        echo "  - tests/e2e/test_knockout_matrix.py (Playwright)"
        echo "  - tests/e2e/test_game_presets_admin.py (Playwright)"
        echo "  - tests/e2e/test_reward_leaderboard_matrix.py (Playwright)"
        echo "These tests run locally with pre-push hooks."
        echo "✅ PASS (skipped - not CI-compatible)"

  payment-workflow-gate:
    name: Payment Workflow E2E (BLOCKING)
    runs-on: ubuntu-latest
    needs: unit-tests

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: lfa_intern_system_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install -r requirements-test.txt
        pip install pytest pytest-xdist
        pip check

    - name: Run DB migrations
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: alembic upgrade head

    - name: Seed tournament types
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: python scripts/seed_tournament_types.py

    - name: Seed LFA test users (@lfa-seed.hu)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: python scripts/seed_lfa_test_users.py

    - name: Start FastAPI backend
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
        TESTING: "true"
      run: |
        uvicorn app.main:app --host 0.0.0.0 --port 8000 &
        # Wait for FastAPI to finish startup (lifespan, scheduler init)
        for i in {1..45}; do
          if curl --fail --silent --max-time 2 http://localhost:8000/health > /dev/null 2>&1; then
            echo "✅ FastAPI ready after ${i} attempts"
            break
          fi
          if [ $i -eq 45 ]; then
            echo "❌ FastAPI failed to start after 45 attempts (90s)"
            exit 1
          fi
          sleep 2
        done

    - name: Run Payment Workflow Tests (Sequential - 0 flake requirement)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        PYTHONPATH=. pytest tests/e2e/integration_critical/test_payment_workflow.py \
          -v \
          --tb=short \
          -ra

    - name: Run Payment Workflow Tests (Parallel - race condition validation)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        PYTHONPATH=. pytest tests/e2e/integration_critical/test_payment_workflow.py \
          -n auto \
          -v \
          --tb=short \
          -ra

    - name: Payment workflow gate failure
      if: failure()
      run: |
        echo "::error::Payment workflow tests failed - merge blocked"
        echo "Revenue-critical path is broken."
        echo "Required: 0 flake, <5s runtime, parallel execution stable"
        exit 1

  core-access-gate:
    name: Core Access & State Sanity (BLOCKING)
    runs-on: ubuntu-latest
    needs: unit-tests

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: lfa_intern_system_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install -r requirements-test.txt
        pip install pytest pytest-xdist
        pip check

    - name: Run DB migrations
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: alembic upgrade head

    - name: Seed tournament types
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: python scripts/seed_tournament_types.py

    - name: Seed LFA test users (@lfa-seed.hu)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: python scripts/seed_lfa_test_users.py

    - name: Start FastAPI backend
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
        TESTING: "true"
      run: |
        uvicorn app.main:app --host 0.0.0.0 --port 8000 &
        # Wait for FastAPI to finish startup (lifespan, scheduler init)
        for i in {1..45}; do
          if curl --fail --silent --max-time 2 http://localhost:8000/health > /dev/null 2>&1; then
            echo "✅ FastAPI ready after ${i} attempts"
            break
          fi
          if [ $i -eq 45 ]; then
            echo "❌ FastAPI failed to start after 45 attempts (90s)"
            exit 1
          fi
          sleep 2
        done

    - name: Run Student + Instructor Lifecycle Tests (Sequential - 0 flake requirement)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        PYTHONPATH=. pytest tests/e2e/integration_critical/test_multi_role_integration.py::test_student_full_enrollment_flow \
          tests/e2e/integration_critical/test_multi_role_integration.py::test_instructor_full_workflow \
          -v \
          --tb=short \
          -ra

    - name: Run Student + Instructor Lifecycle Tests (Parallel - race condition validation)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        PYTHONPATH=. pytest tests/e2e/integration_critical/test_multi_role_integration.py::test_student_full_enrollment_flow \
          tests/e2e/integration_critical/test_multi_role_integration.py::test_instructor_full_workflow \
          -n auto \
          -v \
          --tb=short \
          -ra

    - name: Lifecycle gate failure
      if: failure()
      run: |
        echo "::error::Student + Instructor lifecycle tests failed - merge blocked"
        echo "Core user flows are broken."
        echo "Required: 0 flake, <30s runtime, parallel execution stable"
        exit 1

  student-lifecycle-gate:
    name: Student Lifecycle E2E (BLOCKING)
    runs-on: ubuntu-latest
    needs: unit-tests

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: lfa_intern_system_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install -r requirements-test.txt
        pip install pytest pytest-xdist pytest-repeat
        pip check

    - name: Run DB migrations
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: alembic upgrade head

    - name: Seed tournament types
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: python scripts/seed_tournament_types.py

    - name: Seed LFA test users (@lfa-seed.hu)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: python scripts/seed_lfa_test_users.py

    - name: Start FastAPI backend
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
        TESTING: "true"
      run: |
        uvicorn app.main:app --host 0.0.0.0 --port 8000 &
        # Wait for FastAPI to finish startup (lifespan, scheduler init)
        for i in {1..45}; do
          if curl --fail --silent --max-time 2 http://localhost:8000/health > /dev/null 2>&1; then
            echo "✅ FastAPI ready after ${i} attempts"
            break
          fi
          if [ $i -eq 45 ]; then
            echo "❌ FastAPI failed to start after 45 attempts (90s)"
            exit 1
          fi
          sleep 2
        done

    - name: Run Student Lifecycle Tests (Sequential - 20x validation)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        PYTHONPATH=. pytest tests/e2e/integration_critical/test_student_lifecycle.py \
          --count=20 \
          -v \
          --tb=short \
          -ra

    - name: Run Student Lifecycle Tests (Parallel - race condition validation)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        PYTHONPATH=. pytest tests/e2e/integration_critical/test_student_lifecycle.py \
          -n auto \
          -v \
          --tb=short \
          -ra

    - name: Student lifecycle gate failure
      if: failure()
      run: |
        echo "::error::Student lifecycle tests failed - merge blocked"
        echo "Student enrollment and concurrent safety validation broken."
        echo "Required: 0 flake in 20 runs, <30s runtime, parallel execution stable"
        exit 1

  instructor-lifecycle-gate:
    name: Instructor Lifecycle E2E (BLOCKING)
    runs-on: ubuntu-latest
    needs: unit-tests

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: lfa_intern_system_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install -r requirements-test.txt
        pip install pytest pytest-xdist pytest-repeat
        pip check

    - name: Run DB migrations
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: alembic upgrade head

    - name: Seed tournament types
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: python scripts/seed_tournament_types.py

    - name: Seed LFA test users (@lfa-seed.hu)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: python scripts/seed_lfa_test_users.py

    - name: Start FastAPI backend
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
        TESTING: "true"
      run: |
        uvicorn app.main:app --host 0.0.0.0 --port 8000 &
        # Wait for FastAPI to finish startup (lifespan, scheduler init)
        for i in {1..45}; do
          if curl --fail --silent --max-time 2 http://localhost:8000/health > /dev/null 2>&1; then
            echo "✅ FastAPI ready after ${i} attempts"
            break
          fi
          if [ $i -eq 45 ]; then
            echo "❌ FastAPI failed to start after 45 attempts (90s)"
            exit 1
          fi
          sleep 2
        done

    - name: Run Instructor Lifecycle Tests (Sequential - 20x validation)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        PYTHONPATH=. pytest tests/e2e/integration_critical/test_instructor_lifecycle.py \
          --count=20 \
          -v \
          --tb=short \
          -ra

    - name: Run Instructor Lifecycle Tests (Parallel - race condition validation)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        PYTHONPATH=. pytest tests/e2e/integration_critical/test_instructor_lifecycle.py \
          -n auto \
          -v \
          --tb=short \
          -ra

    - name: Instructor lifecycle gate failure
      if: failure()
      run: |
        echo "::error::Instructor lifecycle tests failed - merge blocked"
        echo "Instructor assignment, check-in, and result submission broken."
        echo "Required: 0 flake in 20 runs, <30s runtime, parallel execution stable"
        exit 1

  refund-workflow-gate:
    name: Refund Workflow E2E (BLOCKING)
    runs-on: ubuntu-latest
    needs: unit-tests

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: lfa_intern_system_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install -r requirements-test.txt
        pip install pytest pytest-xdist pytest-repeat
        pip check

    - name: Run DB migrations
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: alembic upgrade head

    - name: Seed tournament types
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: python scripts/seed_tournament_types.py

    - name: Seed LFA test users (@lfa-seed.hu)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: python scripts/seed_lfa_test_users.py

    - name: Start FastAPI backend
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
        TESTING: "true"
      run: |
        uvicorn app.main:app --host 0.0.0.0 --port 8000 &
        # Wait for FastAPI to finish startup (lifespan, scheduler init)
        for i in {1..45}; do
          if curl --fail --silent --max-time 2 http://localhost:8000/health > /dev/null 2>&1; then
            echo "✅ FastAPI ready after ${i} attempts"
            break
          fi
          if [ $i -eq 45 ]; then
            echo "❌ FastAPI failed to start after 45 attempts (90s)"
            exit 1
          fi
          sleep 2
        done

    - name: Run Refund Workflow Tests (Sequential - 20x validation)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        PYTHONPATH=. pytest tests/e2e/integration_critical/test_refund_workflow.py \
          --count=20 \
          -v \
          --tb=short \
          -ra

    - name: Run Refund Workflow Tests (Parallel - race condition validation)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        PYTHONPATH=. pytest tests/e2e/integration_critical/test_refund_workflow.py \
          -n auto \
          -v \
          --tb=short \
          -ra

    - name: Refund workflow gate failure
      if: failure()
      run: |
        echo "::error::Refund workflow tests failed - merge blocked"
        echo "Revenue-critical refund path broken (50% refund + idempotency + transaction audit)."
        echo "Required: 0 flake in 20 runs, <20s runtime, parallel execution stable"
        exit 1

  multi-campus-gate:
    name: Multi-Campus Round-Robin E2E (BLOCKING)
    runs-on: ubuntu-latest
    needs: unit-tests

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: lfa_intern_system_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install -r requirements-test.txt
        pip install pytest pytest-xdist pytest-repeat
        pip check

    - name: Run DB migrations
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: alembic upgrade head

    - name: Seed tournament types
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: python scripts/seed_tournament_types.py

    - name: Seed LFA test users (@lfa-seed.hu)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: python scripts/seed_lfa_test_users.py

    - name: Start FastAPI backend
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
        TESTING: "true"
      run: |
        uvicorn app.main:app --host 0.0.0.0 --port 8000 &
        # Wait for FastAPI to finish startup (lifespan, scheduler init)
        for i in {1..45}; do
          if curl --fail --silent --max-time 2 http://localhost:8000/health > /dev/null 2>&1; then
            echo "✅ FastAPI ready after ${i} attempts"
            break
          fi
          if [ $i -eq 45 ]; then
            echo "❌ FastAPI failed to start after 45 attempts (90s)"
            exit 1
          fi
          sleep 2
        done

    - name: Run Multi-Campus Tests (Sequential - 20x validation)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        PYTHONPATH=. pytest tests/e2e/integration_critical/test_multi_campus.py \
          --count=20 \
          -v \
          --tb=short \
          -ra

    - name: Run Multi-Campus Tests (Parallel - race condition validation)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        PYTHONPATH=. pytest tests/e2e/integration_critical/test_multi_campus.py \
          -n auto \
          -v \
          --tb=short \
          -ra

    - name: Multi-campus gate failure
      if: failure()
      run: |
        echo "::error::Multi-campus tests failed - merge blocked"
        echo "Multi-campus infrastructure and round-robin distribution broken."
        echo "Required: 0 flake in 20 runs, <30s runtime, parallel execution stable"
        exit 1

  session-management-gate:
    name: Session Management E2E (BLOCKING)
    runs-on: ubuntu-latest
    needs: unit-tests

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: lfa_intern_system_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install -r requirements-test.txt
        pip install pytest pytest-xdist pytest-repeat
        pip check

    - name: Run DB migrations
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: alembic upgrade head

    - name: Seed tournament types
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: python scripts/seed_tournament_types.py

    - name: Seed LFA test users (@lfa-seed.hu)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: python scripts/seed_lfa_test_users.py

    - name: Start FastAPI backend
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
        TESTING: "true"
      run: |
        uvicorn app.main:app --host 0.0.0.0 --port 8000 &
        # Wait for FastAPI to finish startup (lifespan, scheduler init)
        for i in {1..45}; do
          if curl --fail --silent --max-time 2 http://localhost:8000/health > /dev/null 2>&1; then
            echo "✅ FastAPI ready after ${i} attempts"
            break
          fi
          if [ $i -eq 45 ]; then
            echo "❌ FastAPI failed to start after 45 attempts (90s)"
            exit 1
          fi
          sleep 2
        done

    - name: Run Session Management Tests (Sequential - 20x validation)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        PYTHONPATH=. pytest app/tests/test_session_management_e2e.py \
          --count=20 \
          -v \
          --tb=short \
          -ra

    - name: Run Session Management Tests (Parallel - race condition validation)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        PYTHONPATH=. pytest app/tests/test_session_management_e2e.py \
          -n auto \
          -v \
          --tb=short \
          -ra

    - name: Performance check (p95 < 200ms for check-in)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        PYTHONPATH=. pytest app/tests/test_session_management_e2e.py \
          --durations=10 \
          -v

    - name: Session management gate failure
      if: failure()
      run: |
        echo "::error::Session management tests failed - merge blocked"
        echo "Session lifecycle (check-in, capacity, authorization) broken."
        echo "Required: 0 flake in 20 runs, <5s runtime, parallel execution stable, p95 check-in <200ms"
        exit 1

  skill-assessment-lifecycle-gate:
    name: Skill Assessment Lifecycle E2E (BLOCKING)
    runs-on: ubuntu-latest
    needs: unit-tests

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: lfa_intern_system_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install -r requirements-test.txt
        pip install pytest pytest-xdist pytest-repeat
        pip check

    - name: Run DB migrations
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: alembic upgrade head

    - name: Run Skill Assessment Lifecycle Tests (Sequential - 20x validation)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        PYTHONPATH=. pytest tests/e2e/integration_critical/test_skill_assessment_lifecycle.py \
          --count=20 \
          -v \
          --tb=short \
          -ra \
          --durations=5

    - name: Run Skill Assessment Lifecycle Tests (Parallel - race condition validation)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        PYTHONPATH=. pytest tests/e2e/integration_critical/test_skill_assessment_lifecycle.py \
          -n auto \
          -v \
          --tb=short \
          -ra

    - name: Skill assessment lifecycle gate failure
      if: failure()
      run: |
        echo "::error::Skill assessment lifecycle tests failed - merge blocked"
        echo "Skill assessment state machine (NOT_ASSESSED → ASSESSED → VALIDATED → ARCHIVED) broken."
        echo "Required: 0 flake in 20 runs, <5s runtime, parallel execution stable"
        exit 1

  booking-lifecycle-gate:
    name: Booking Lifecycle E2E (BLOCKING)
    runs-on: ubuntu-latest
    needs: unit-tests

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: lfa_intern_system_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install -r requirements-test.txt
        pip install pytest pytest-xdist pytest-repeat
        pip check

    - name: Run DB migrations
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: alembic upgrade head

    - name: Seed tournament types
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: python scripts/seed_tournament_types.py

    - name: Seed LFA test users (@lfa-seed.hu)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: python scripts/seed_lfa_test_users.py

    - name: Start FastAPI backend
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
        TESTING: "true"
      run: |
        uvicorn app.main:app --host 0.0.0.0 --port 8000 &
        # Wait for FastAPI to finish startup (lifespan, scheduler init)
        for i in {1..45}; do
          if curl --fail --silent --max-time 2 http://localhost:8000/health > /dev/null 2>&1; then
            echo "✅ FastAPI ready after ${i} attempts"
            break
          fi
          if [ $i -eq 45 ]; then
            echo "❌ FastAPI failed to start after 45 attempts (90s)"
            exit 1
          fi
          sleep 2
        done

    - name: Run Booking Lifecycle Tests (Sequential - 0 flake requirement)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        PYTHONPATH=. pytest tests/e2e/integration_critical/test_booking_lifecycle.py \
          --count=3 \
          -v \
          --tb=short \
          -ra

    - name: Run Booking Lifecycle Tests (Parallel - race condition validation)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        PYTHONPATH=. pytest tests/e2e/integration_critical/test_booking_lifecycle.py \
          -n auto \
          -v \
          --tb=short \
          -ra

    - name: Booking lifecycle gate failure
      if: failure()
      run: |
        echo "::error::Booking lifecycle tests failed - merge blocked"
        echo "Core revenue flow (confirm → waitlist → cancel → auto-promote) is broken."
        echo "Required: 0 flake, parallel execution stable, no overbooking"
        exit 1

  auth-lifecycle-gate:
    name: Auth Lifecycle E2E (BLOCKING)
    runs-on: ubuntu-latest
    needs: unit-tests

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: lfa_intern_system_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install -r requirements-test.txt
        pip install pytest pytest-xdist pytest-repeat
        pip check

    - name: Run DB migrations
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: alembic upgrade head

    - name: Seed tournament types
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: python scripts/seed_tournament_types.py

    - name: Seed LFA test users (@lfa-seed.hu)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: python scripts/seed_lfa_test_users.py

    - name: Start FastAPI backend
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
        TESTING: "true"
      run: |
        uvicorn app.main:app --host 0.0.0.0 --port 8000 &
        for i in {1..45}; do
          if curl --fail --silent --max-time 2 http://localhost:8000/health > /dev/null 2>&1; then
            echo "✅ FastAPI ready after ${i} attempts"
            break
          fi
          if [ $i -eq 45 ]; then
            echo "❌ FastAPI failed to start after 45 attempts (90s)"
            exit 1
          fi
          sleep 2
        done

    - name: Run Auth Lifecycle Tests (Sequential - 0 flake requirement)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        PYTHONPATH=. pytest tests/e2e/integration_critical/test_auth_lifecycle.py \
          --count=3 \
          -v \
          --tb=short \
          -ra

    - name: Run Auth Lifecycle Tests (Parallel - race condition validation)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        PYTHONPATH=. pytest tests/e2e/integration_critical/test_auth_lifecycle.py \
          -n auto \
          -v \
          --tb=short \
          -ra

    - name: Auth lifecycle gate failure
      if: failure()
      run: |
        echo "::error::Auth lifecycle tests failed - merge blocked"
        echo "Core auth flow (login → token → refresh → protected endpoint) is broken."
        echo "Required: 0 flake, parallel execution stable"
        exit 1

  enrollment-workflow-gate:
    name: Enrollment Workflow E2E (BLOCKING)
    runs-on: ubuntu-latest
    needs: unit-tests

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: lfa_intern_system_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install -r requirements-test.txt
        pip install pytest pytest-xdist pytest-repeat
        pip check

    - name: Run DB migrations
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: alembic upgrade head

    - name: Seed tournament types
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: python scripts/seed_tournament_types.py

    - name: Seed LFA test users (@lfa-seed.hu)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: python scripts/seed_lfa_test_users.py

    - name: Start FastAPI backend
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
        TESTING: "true"
      run: |
        uvicorn app.main:app --host 0.0.0.0 --port 8000 &
        for i in {1..45}; do
          if curl --fail --silent --max-time 2 http://localhost:8000/health > /dev/null 2>&1; then
            echo "✅ FastAPI ready after ${i} attempts"
            break
          fi
          if [ $i -eq 45 ]; then
            echo "❌ FastAPI failed to start after 45 attempts (90s)"
            exit 1
          fi
          sleep 2
        done

    - name: Run Enrollment Workflow Tests (Sequential - 0 flake requirement)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        PYTHONPATH=. pytest tests/e2e/integration_critical/test_enrollment_workflow.py \
          --count=3 \
          -v \
          --tb=short \
          -ra

    - name: Run Enrollment Workflow Tests (Parallel - race condition validation)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        PYTHONPATH=. pytest tests/e2e/integration_critical/test_enrollment_workflow.py \
          -n auto \
          -v \
          --tb=short \
          -ra

    - name: Enrollment workflow gate failure
      if: failure()
      run: |
        echo "::error::Enrollment workflow tests failed - merge blocked"
        echo "Core enrollment flow (enroll → auto-approve → admin list → duplicate rejected) is broken."
        echo "Required: 0 flake, parallel execution stable"
        exit 1

  user-account-gate:
    name: User Account E2E (BLOCKING)
    runs-on: ubuntu-latest
    needs: unit-tests

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: lfa_intern_system_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install -r requirements-test.txt
        pip install pytest pytest-xdist pytest-repeat
        pip check

    - name: Run DB migrations
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: alembic upgrade head

    - name: Seed tournament types
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: python scripts/seed_tournament_types.py

    - name: Seed LFA test users (@lfa-seed.hu)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: python scripts/seed_lfa_test_users.py

    - name: Start FastAPI backend
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
        TESTING: "true"
      run: |
        uvicorn app.main:app --host 0.0.0.0 --port 8000 &
        for i in {1..45}; do
          if curl --fail --silent --max-time 2 http://localhost:8000/health > /dev/null 2>&1; then
            echo "✅ FastAPI ready after ${i} attempts"
            break
          fi
          if [ $i -eq 45 ]; then
            echo "❌ FastAPI failed to start after 45 attempts (90s)"
            exit 1
          fi
          sleep 2
        done

    - name: Run User Account Tests (Sequential - flake detection)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        PYTHONPATH=. pytest tests/e2e/integration_critical/test_user_account.py \
          --count=3 \
          -v \
          --tb=short \
          -ra

    - name: Run User Account Tests (Parallel - race condition validation)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        PYTHONPATH=. pytest tests/e2e/integration_critical/test_user_account.py \
          -n auto \
          -v \
          --tb=short \
          -ra

    - name: User account gate failure
      if: failure()
      run: |
        echo "::error::User account tests failed - merge blocked"
        echo "Password change security flow (change → old creds fail → new creds work) is broken."
        echo "Required: 0 flake in 3 runs, parallel execution stable"
        exit 1

  instructor-assignment-lifecycle-gate:
    name: Instructor Assignment Lifecycle E2E (BLOCKING)
    runs-on: ubuntu-latest
    needs: unit-tests

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: lfa_intern_system_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install -r requirements-test.txt
        pip install pytest pytest-xdist pytest-repeat
        pip check

    - name: Run DB migrations
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: alembic upgrade head

    - name: Seed tournament types
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: python scripts/seed_tournament_types.py

    - name: Seed LFA test users (@lfa-seed.hu)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: python scripts/seed_lfa_test_users.py

    - name: Start FastAPI backend
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
        TESTING: "true"
      run: |
        uvicorn app.main:app --host 0.0.0.0 --port 8000 &
        for i in {1..45}; do
          if curl --fail --silent --max-time 2 http://localhost:8000/health > /dev/null 2>&1; then
            echo "✅ FastAPI ready after ${i} attempts"
            break
          fi
          if [ $i -eq 45 ]; then
            echo "❌ FastAPI failed to start after 45 attempts (90s)"
            exit 1
          fi
          sleep 2
        done

    - name: Run Instructor Assignment Tests (Sequential - flake detection)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        PYTHONPATH=. pytest tests/e2e/integration_critical/test_instructor_assignment_lifecycle.py \
          --count=3 \
          -v \
          --tb=short \
          -ra

    - name: Run Instructor Assignment Tests (Parallel - race condition validation)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_intern_system_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        PYTHONPATH=. pytest tests/e2e/integration_critical/test_instructor_assignment_lifecycle.py \
          -n auto \
          -v \
          --tb=short \
          -ra

    - name: Instructor assignment lifecycle gate failure
      if: failure()
      run: |
        echo "::error::Instructor assignment lifecycle tests failed - merge blocked"
        echo "Cycle 2 assignment flow (admin assigns → instructor accepts/declines) is broken."
        echo "Required: 0 flake in 3 runs, parallel execution stable"
        exit 1

  migration-integrity:
    name: "Migration Chain Integrity (empty → head → base → head)"
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: lfa_migration_integrity_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Upgrade from empty database to head
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_migration_integrity_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        alembic upgrade head
        CURRENT=$(alembic current 2>&1)
        echo "After upgrade: $CURRENT"
        if ! echo "$CURRENT" | grep -q "(head)"; then
          echo "❌ Not at head after upgrade from empty DB"
          exit 1
        fi
        echo "✅ Empty → head: OK"

    - name: Full round-trip downgrade to base then upgrade to head
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_migration_integrity_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        alembic downgrade base
        echo "✅ Downgrade to base: OK"
        alembic upgrade head
        CURRENT=$(alembic current 2>&1)
        echo "After round-trip: $CURRENT"
        if ! echo "$CURRENT" | grep -q "(head)"; then
          echo "❌ Not at head after base → head round-trip"
          exit 1
        fi
        echo "✅ base → head round-trip: OK"

    - name: Verify DB constraints (uq_user_license_spec)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_migration_integrity_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        python scripts/validate_db_constraints.py --exit-code
        echo "✅ DB constraints verified"

    - name: Migration chain failure annotation
      if: failure()
      run: |
        echo "::error::Migration chain integrity failed."
        echo "A migration may have broken references, invalid SQL, or missing dependency."
        echo "Run locally: alembic downgrade base && alembic upgrade head"
        exit 1

  migration-volume-test:
    # Validates that migrations run within acceptable time even when the
    # database already contains a production-like volume of rows.  Catches
    # migrations that are fast on an empty DB but would time out on real data
    # (e.g. missing index on a large table, full-table dedup scan, etc.).
    name: "Migration Volume Safety (data-present roundtrip)"
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: lfa_migration_volume_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run migrations on empty database
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_migration_volume_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        alembic upgrade head
        echo "✅ Empty → head: OK"

    - name: Seed production-like dataset (2 000 bookings, 500 reward rows)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_migration_volume_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        python scripts/seed_volume_test.py
        echo "✅ Volume dataset seeded"

    - name: Verify index effectiveness on seeded data
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_migration_volume_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        # Check that the capacity-check query uses the composite index
        # EXPLAIN ANALYZE output must show Index Scan (not Seq Scan) on bookings
        python - <<'EOF'
        import os, sys
        from sqlalchemy import create_engine, text
        engine = create_engine(os.environ["DATABASE_URL"])
        with engine.connect() as conn:
            plan = conn.execute(text(
                "EXPLAIN ANALYZE SELECT COUNT(*) FROM bookings "
                "WHERE session_id = 1 AND status = 'CONFIRMED'"
            )).fetchall()
        plan_text = "\n".join(row[0] for row in plan)
        print(plan_text)
        # With the composite index present, PostgreSQL should choose an Index Scan
        if "Seq Scan on bookings" in plan_text and "rows=2000" not in plan_text:
            print("⚠️  Seq Scan detected — index may be missing or not selective enough")
        else:
            print("✅ Query plan is index-efficient")
        EOF

    - name: Downgrade one step then re-upgrade on data-present DB
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_migration_volume_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        START_NS=$(date +%s%N)
        # alembic downgrade -1 is ambiguous at merge migration points (two parents).
        # Resolve the parent revision dynamically: handles both regular and merge heads.
        PREV=$(python3 scripts/get_prev_migration.py)
        echo "Downgrading to prev revision: $PREV"
        alembic downgrade "$PREV"
        alembic upgrade head
        END_NS=$(date +%s%N)
        ELAPSED_MS=$(( (END_NS - START_NS) / 1000000 ))
        echo "Migration roundtrip on seeded DB: ${ELAPSED_MS}ms"
        if [ "$ELAPSED_MS" -gt 30000 ]; then
          echo "::error::Migration roundtrip took ${ELAPSED_MS}ms (> 30s limit on seeded dataset)"
          exit 1
        fi
        echo "Migration roundtrip on seeded DB: ${ELAPSED_MS}ms (within 30s limit)"

    - name: Migration volume failure annotation
      if: failure()
      run: |
        echo "::error::Migration volume safety check failed."
        echo "A migration step may perform full-table scans or deduplication that"
        echo "degrades linearly with row count.  Add an index or batch the operation."
        exit 1

  security-gate:
    # Validates that CSRF double-submit cookie protection and SQL injection
    # prevention are intact.  Runs against a live server (requests-based tests).
    # Excluded: test_cors_config.py — references Streamlit port 8501 (decommissioned Sprint 59h)
    name: "Security Gate (CSRF + SQL injection — 63 tests)"
    runs-on: ubuntu-latest
    needs: unit-tests

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: lfa_security_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install -r requirements-test.txt

    - name: Run DB migrations
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_security_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
        ADMIN_EMAIL: admin@lfa.com
        ADMIN_PASSWORD: admin123
        ENVIRONMENT: test
        TESTING: "true"
        ENABLE_RATE_LIMITING: "false"
      run: alembic upgrade head

    - name: Seed security test users
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_security_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
        ADMIN_EMAIL: admin@lfa.com
        ADMIN_PASSWORD: admin123
        ENVIRONMENT: test
        TESTING: "true"
        ENABLE_RATE_LIMITING: "false"
      run: |
        python scripts/seed_e2e_users.py
        echo "✅ Security test users seeded"

    - name: Start application server
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_security_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
        ADMIN_EMAIL: admin@lfa.com
        ADMIN_PASSWORD: admin123
        ENVIRONMENT: test
        TESTING: "true"
        ENABLE_RATE_LIMITING: "false"
        DEBUG: "false"
      run: |
        nohup uvicorn app.main:app --host 127.0.0.1 --port 8000 --log-level warning &
        timeout 40 bash -c 'until curl -sf http://127.0.0.1:8000/health; do sleep 1; done'
        echo "✅ Server ready"

    - name: CSRF protection tests (Double Submit Cookie — 28 tests)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_security_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
        ADMIN_EMAIL: admin@lfa.com
        ADMIN_PASSWORD: admin123
        ENVIRONMENT: test
        TESTING: "true"
        ENABLE_RATE_LIMITING: "false"
      run: |
        # test_cors_config.py excluded: references Streamlit :8501 (decommissioned Sprint 59h)
        PYTHONPATH=. pytest \
          tests/security/csrf/test_csrf_tokens.py \
          tests/security/csrf/test_cookie_security.py \
          -v --tb=short --maxfail=0

    - name: SQL injection prevention tests (35 tests)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_security_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
        ADMIN_EMAIL: admin@lfa.com
        ADMIN_PASSWORD: admin123
        ENVIRONMENT: test
        TESTING: "true"
        ENABLE_RATE_LIMITING: "false"
      run: |
        PYTHONPATH=. pytest tests/security/sql_injection/ \
          -v --tb=short --maxfail=0

    - name: Security gate failure annotation
      if: failure()
      run: |
        echo "::error::Security gate FAILED — CSRF or SQL injection protection regression detected."
        echo "A security control may have been removed or weakened."
        echo "Review failing tests before merging."
        exit 1

  operational-smoke-test:
    # Starts the full application stack (API + PostgreSQL + Redis) and runs a
    # short smoke test against the live HTTP endpoints.  Validates that:
    #   - Migrations apply cleanly on a fresh DB
    #   - The app becomes ready within 40 s (/health/ready → 200)
    #   - Core endpoints are reachable and return expected JSON structure
    #   - Prometheus export is available with text/plain content-type
    name: "Operational Smoke Test (full stack)"
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: lfa_smoke_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

      redis:
        image: redis:7
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 6379:6379

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run migrations
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_smoke_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        alembic upgrade head
        echo "✅ Migrations applied"

    - name: Seed tournament types
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_smoke_test
        SECRET_KEY: test-secret-key-for-ci-only-not-production
      run: |
        python scripts/seed_tournament_types.py
        echo "✅ Tournament types seeded"

    - name: Start application server
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lfa_smoke_test
        REDIS_URL: redis://localhost:6379/0
        CELERY_BROKER_URL: redis://localhost:6379/0
        CELERY_RESULT_BACKEND: redis://localhost:6379/1
        SECRET_KEY: test-secret-key-for-ci-only-not-production
        ADMIN_EMAIL: smoke@test.com
        ADMIN_PASSWORD: SmokeTestPass123!
        ENVIRONMENT: development
        ENABLE_RATE_LIMITING: "false"
      run: |
        nohup uvicorn app.main:app --host 127.0.0.1 --port 8000 --log-level warning &
        echo "SERVER_PID=$!" >> $GITHUB_ENV
        echo "Server started with PID $!"

    - name: Wait for /health/ready (up to 40 s)
      run: |
        STATUS="000"
        for i in $(seq 1 20); do
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8000/health/ready || echo "000")
          if [ "$STATUS" == "200" ]; then
            echo "✅ /health/ready returned 200 (attempt $i)"
            break
          fi
          echo "Attempt $i: HTTP $STATUS — retrying in 2 s"
          sleep 2
        done
        if [ "$STATUS" != "200" ]; then
          echo "::error::Application did not become ready within 40 seconds (last HTTP status: $STATUS)"
          exit 1
        fi

    - name: API smoke checks
      run: |
        python - <<'PYEOF'
        import sys, json, urllib.request, urllib.error

        BASE = "http://127.0.0.1:8000"
        failures = []

        def check(label, url, expected_status, required_keys=None):
            try:
                with urllib.request.urlopen(url, timeout=10) as resp:
                    status = resp.status
                    body = json.loads(resp.read())
            except urllib.error.HTTPError as exc:
                status = exc.code
                body = {}
            except Exception as exc:
                failures.append(f"FAIL {label}: {exc}")
                return
            if status != expected_status:
                failures.append(f"FAIL {label}: expected HTTP {expected_status}, got {status}")
                return
            if required_keys:
                missing = [k for k in required_keys if k not in body]
                if missing:
                    failures.append(f"FAIL {label}: missing keys {missing}")
                    return
            print(f"  PASS {label}: HTTP {status}")

        # Health endpoints
        check("GET /health/ready",   f"{BASE}/health/ready",   200, ["status"])
        check("GET /health/worker",  f"{BASE}/health/worker",  200)

        # Metrics endpoints
        check("GET /metrics (json)",  f"{BASE}/metrics",         200, ["counters", "labeled_counters"])
        check("GET /metrics/alerts",  f"{BASE}/metrics/alerts",  200, ["status", "thresholds"])

        # Prometheus text format
        try:
            with urllib.request.urlopen(f"{BASE}/metrics?format=prometheus", timeout=10) as resp:
                ctype = resp.headers.get("Content-Type", "")
                text = resp.read().decode()
            if "text/plain" not in ctype:
                failures.append(f"FAIL GET /metrics?format=prometheus: expected text/plain, got {ctype!r}")
            else:
                print(f"  PASS GET /metrics?format=prometheus: content-type OK")
        except Exception as exc:
            failures.append(f"FAIL GET /metrics?format=prometheus: {exc}")

        # OpenAPI schema
        try:
            with urllib.request.urlopen(f"{BASE}/api/v1/openapi.json", timeout=10) as resp:
                schema = json.loads(resp.read())
            path_count = len(schema.get("paths", {}))
            if path_count < 100:
                failures.append(f"FAIL GET /api/v1/openapi.json: only {path_count} paths (expected ≥ 100)")
            else:
                print(f"  PASS GET /api/v1/openapi.json: {path_count} paths")
        except Exception as exc:
            failures.append(f"FAIL GET /api/v1/openapi.json: {exc}")

        if failures:
            print("\n❌ Smoke check failures:")
            for f in failures:
                print(f"  {f}")
            sys.exit(1)
        else:
            print(f"\n✅ All operational smoke checks passed ({6 - len(failures)}/6)")
        PYEOF

    - name: Collect application logs on failure
      if: failure()
      run: |
        echo "=== Application logs (nohup.out) ==="
        cat nohup.out 2>/dev/null || echo "(no nohup.out found)"

    - name: Operational smoke test failure annotation
      if: failure()
      run: |
        echo "::error::Operational smoke test failed."
        echo "The application stack did not start cleanly or a core endpoint returned"
        echo "an unexpected status code.  Check the 'Collect application logs' step above."

  # ─── CYPRESS E2E WEB SUITE (GATE C) ──────────────────────────────────────────
  # Inlined from cypress-web-e2e.yml to produce a single unified run ID.
  # All 5 jobs must pass (0 skipped allowed) — Gate C of EXECUTION_TRUTH_MODEL_v1.

  cypress-web-admin:
    name: Cypress Web E2E — admin
    runs-on: ubuntu-latest
    needs: unit-tests

    services:
      postgres:
        image: postgres:14
        env:
          POSTGRES_DB: lfa_test
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install Python dependencies
        run: pip install -r requirements.txt
      - name: Write .env
        run: |
          cat > .env << 'EOF'
          DATABASE_URL=postgresql://postgres:postgres@localhost:5432/lfa_test
          SECRET_KEY=ci-web-e2e-key-not-for-production
          ENVIRONMENT=test
          TESTING=true
          DEBUG=false
          ENABLE_RATE_LIMITING=false
          EOF
      - name: Run Alembic migrations
        run: alembic upgrade head
      - name: Seed DB — baseline
        run: python scripts/reset_e2e_web_db.py --scenario baseline
      - name: Start FastAPI backend
        run: |
          uvicorn app.main:app --host 0.0.0.0 --port 8000 &
          timeout 90 bash -c 'until curl -sf http://localhost:8000/health; do sleep 1; done'
          echo "FastAPI backend ready"
      - name: Install Cypress
        working-directory: cypress
        run: npm ci
      - name: Run Cypress — admin
        uses: cypress-io/github-action@v6
        with:
          working-directory: cypress
          spec: cypress/e2e/web/admin/**
          browser: chrome
          config: "baseUrl=http://localhost:8000"
        env:
          CYPRESS_webAdminEmail: admin@lfa.com
          CYPRESS_webAdminPassword: admin123
          CYPRESS_webInstructorEmail: grandmaster@lfa.com
          CYPRESS_webInstructorPassword: TestInstructor2026
          CYPRESS_webStudentEmail: rdias@manchestercity.com
          CYPRESS_webStudentPassword: TestPlayer2026
          CYPRESS_webFreshEmail: fresh.e2e@lfa.com
          CYPRESS_webFreshPassword: FreshE2E2026
          CYPRESS_apiUrl: http://localhost:8000
      - name: Upload screenshots on failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: cypress-screenshots-admin
          path: cypress/cypress/screenshots
          retention-days: 7

  cypress-web-instructor:
    name: Cypress Web E2E — instructor
    runs-on: ubuntu-latest
    needs: unit-tests

    services:
      postgres:
        image: postgres:14
        env:
          POSTGRES_DB: lfa_test
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install Python dependencies
        run: pip install -r requirements.txt
      - name: Write .env
        run: |
          cat > .env << 'EOF'
          DATABASE_URL=postgresql://postgres:postgres@localhost:5432/lfa_test
          SECRET_KEY=ci-web-e2e-key-not-for-production
          ENVIRONMENT=test
          TESTING=true
          DEBUG=false
          ENABLE_RATE_LIMITING=false
          EOF
      - name: Run Alembic migrations
        run: alembic upgrade head
      - name: Seed DB — session_ready
        run: python scripts/reset_e2e_web_db.py --scenario session_ready
      - name: Start FastAPI backend
        run: |
          uvicorn app.main:app --host 0.0.0.0 --port 8000 &
          timeout 90 bash -c 'until curl -sf http://localhost:8000/health; do sleep 1; done'
          echo "FastAPI backend ready"
      - name: Install Cypress
        working-directory: cypress
        run: npm ci
      - name: Run Cypress — instructor
        uses: cypress-io/github-action@v6
        with:
          working-directory: cypress
          spec: "cypress/e2e/web/instructor/**,cypress/e2e/web/sessions/instructor_session_lifecycle.cy.js"
          browser: chrome
          config: "baseUrl=http://localhost:8000"
        env:
          CYPRESS_webAdminEmail: admin@lfa.com
          CYPRESS_webAdminPassword: admin123
          CYPRESS_webInstructorEmail: grandmaster@lfa.com
          CYPRESS_webInstructorPassword: TestInstructor2026
          CYPRESS_webStudentEmail: rdias@manchestercity.com
          CYPRESS_webStudentPassword: TestPlayer2026
          CYPRESS_webFreshEmail: fresh.e2e@lfa.com
          CYPRESS_webFreshPassword: FreshE2E2026
          CYPRESS_apiUrl: http://localhost:8000
      - name: Upload screenshots on failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: cypress-screenshots-instructor
          path: cypress/cypress/screenshots
          retention-days: 7

  cypress-web-student:
    name: Cypress Web E2E — student
    runs-on: ubuntu-latest
    needs: unit-tests

    services:
      postgres:
        image: postgres:14
        env:
          POSTGRES_DB: lfa_test
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install Python dependencies
        run: pip install -r requirements.txt
      - name: Write .env
        run: |
          cat > .env << 'EOF'
          DATABASE_URL=postgresql://postgres:postgres@localhost:5432/lfa_test
          SECRET_KEY=ci-web-e2e-key-not-for-production
          ENVIRONMENT=test
          TESTING=true
          DEBUG=false
          ENABLE_RATE_LIMITING=false
          EOF
      - name: Run Alembic migrations
        run: alembic upgrade head
      - name: Seed DB — student_with_credits
        run: python scripts/reset_e2e_web_db.py --scenario student_with_credits
      - name: Start FastAPI backend
        run: |
          uvicorn app.main:app --host 0.0.0.0 --port 8000 &
          timeout 90 bash -c 'until curl -sf http://localhost:8000/health; do sleep 1; done'
          echo "FastAPI backend ready"
      - name: Install Cypress
        working-directory: cypress
        run: npm ci
      - name: Run Cypress — student
        uses: cypress-io/github-action@v6
        with:
          working-directory: cypress
          spec: "cypress/e2e/web/auth/**,cypress/e2e/web/student/**,cypress/e2e/web/sessions/student_booking.cy.js"
          browser: chrome
          config: "baseUrl=http://localhost:8000"
        env:
          CYPRESS_webAdminEmail: admin@lfa.com
          CYPRESS_webAdminPassword: admin123
          CYPRESS_webInstructorEmail: grandmaster@lfa.com
          CYPRESS_webInstructorPassword: TestInstructor2026
          CYPRESS_webStudentEmail: rdias@manchestercity.com
          CYPRESS_webStudentPassword: TestPlayer2026
          CYPRESS_webFreshEmail: fresh.e2e@lfa.com
          CYPRESS_webFreshPassword: FreshE2E2026
          CYPRESS_apiUrl: http://localhost:8000
      - name: Upload screenshots on failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: cypress-screenshots-student
          path: cypress/cypress/screenshots
          retention-days: 7

  cypress-web-business:
    name: Cypress Web E2E — business-workflow
    runs-on: ubuntu-latest
    needs: unit-tests

    services:
      postgres:
        image: postgres:14
        env:
          POSTGRES_DB: lfa_test
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install Python dependencies
        run: pip install -r requirements.txt
      - name: Write .env
        run: |
          cat > .env << 'EOF'
          DATABASE_URL=postgresql://postgres:postgres@localhost:5432/lfa_test
          SECRET_KEY=ci-web-e2e-key-not-for-production
          ENVIRONMENT=test
          TESTING=true
          DEBUG=false
          ENABLE_RATE_LIMITING=false
          EOF
      - name: Run Alembic migrations
        run: alembic upgrade head
      - name: Seed DB — business_lifecycle
        run: python scripts/reset_e2e_web_db.py --scenario business_lifecycle
      - name: Start FastAPI backend
        run: |
          uvicorn app.main:app --host 0.0.0.0 --port 8000 &
          timeout 90 bash -c 'until curl -sf http://localhost:8000/health; do sleep 1; done'
          echo "FastAPI backend ready"
      - name: Install Cypress
        working-directory: cypress
        run: npm ci
      - name: Run Cypress — business-workflow
        uses: cypress-io/github-action@v6
        with:
          working-directory: cypress
          spec: cypress/e2e/web/business_workflows/**
          browser: chrome
          config: "baseUrl=http://localhost:8000"
        env:
          CYPRESS_webAdminEmail: admin@lfa.com
          CYPRESS_webAdminPassword: admin123
          CYPRESS_webInstructorEmail: grandmaster@lfa.com
          CYPRESS_webInstructorPassword: TestInstructor2026
          CYPRESS_webStudentEmail: rdias@manchestercity.com
          CYPRESS_webStudentPassword: TestPlayer2026
          CYPRESS_webFreshEmail: fresh.e2e@lfa.com
          CYPRESS_webFreshPassword: FreshE2E2026
          CYPRESS_apiUrl: http://localhost:8000
      - name: Upload screenshots on failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: cypress-screenshots-business
          path: cypress/cypress/screenshots
          retention-days: 7

  cypress-web-integration:
    name: Cypress Web E2E — cross-role integration
    runs-on: ubuntu-latest
    needs: [cypress-web-admin, cypress-web-instructor, cypress-web-student, cypress-web-business]

    services:
      postgres:
        image: postgres:14
        env:
          POSTGRES_DB: lfa_test
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install Python dependencies
        run: pip install -r requirements.txt
      - name: Write .env
        run: |
          cat > .env << 'EOF'
          DATABASE_URL=postgresql://postgres:postgres@localhost:5432/lfa_test
          SECRET_KEY=ci-web-e2e-key-not-for-production
          ENVIRONMENT=test
          TESTING=true
          DEBUG=false
          ENABLE_RATE_LIMITING=false
          EOF
      - name: Run Alembic migrations
        run: alembic upgrade head
      - name: Seed DB — session_ready (cross-role lifecycle)
        run: python scripts/reset_e2e_web_db.py --scenario session_ready
      - name: Start FastAPI backend
        run: |
          uvicorn app.main:app --host 0.0.0.0 --port 8000 &
          timeout 90 bash -c 'until curl -sf http://localhost:8000/health; do sleep 1; done'
          echo "FastAPI backend ready"
      - name: Install Cypress
        working-directory: cypress
        run: npm ci
      - name: Run cross-role lifecycle test
        uses: cypress-io/github-action@v6
        with:
          working-directory: cypress
          spec: cypress/e2e/web/cross_role/full_student_lifecycle.cy.js
          browser: chrome
          config: "baseUrl=http://localhost:8000"
        env:
          CYPRESS_webAdminEmail: admin@lfa.com
          CYPRESS_webAdminPassword: admin123
          CYPRESS_webInstructorEmail: grandmaster@lfa.com
          CYPRESS_webInstructorPassword: TestInstructor2026
          CYPRESS_webStudentEmail: rdias@manchestercity.com
          CYPRESS_webStudentPassword: TestPlayer2026
          CYPRESS_webFreshEmail: fresh.e2e@lfa.com
          CYPRESS_webFreshPassword: FreshE2E2026
          CYPRESS_apiUrl: http://localhost:8000
      - name: Upload screenshots on failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: cypress-screenshots-integration
          path: cypress/cypress/screenshots
          retention-days: 7

  baseline-report:
    name: Generate Baseline Report
    runs-on: ubuntu-latest
    needs: [unit-tests, smoke-tests, api-module-integrity, hardcoded-id-guard, credit-audit, security-gate, migration-integrity, migration-volume-test, payment-workflow-gate, core-access-gate, student-lifecycle-gate, instructor-lifecycle-gate, refund-workflow-gate, multi-campus-gate, session-management-gate, skill-assessment-lifecycle-gate, booking-lifecycle-gate, auth-lifecycle-gate, enrollment-workflow-gate, user-account-gate, instructor-assignment-lifecycle-gate, operational-smoke-test, cypress-web-admin, cypress-web-instructor, cypress-web-student, cypress-web-business, cypress-web-integration]
    if: always()

    steps:
    - uses: actions/checkout@v4

    - name: Baseline Status
      run: |
        echo "## Test Baseline Status" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY

        UNIT="${{ needs.unit-tests.result }}"
        SMOKE="${{ needs.smoke-tests.result }}"
        INTEGRITY="${{ needs.api-module-integrity.result }}"
        ID_GUARD="${{ needs.hardcoded-id-guard.result }}"
        CREDIT_AUDIT="${{ needs.credit-audit.result }}"
        SECURITY="${{ needs.security-gate.result }}"
        MIGRATION="${{ needs.migration-integrity.result }}"
        PAYMENT="${{ needs.payment-workflow-gate.result }}"
        CORE_ACCESS="${{ needs.core-access-gate.result }}"
        STUDENT_LIFECYCLE="${{ needs.student-lifecycle-gate.result }}"
        INSTRUCTOR_LIFECYCLE="${{ needs.instructor-lifecycle-gate.result }}"
        REFUND_WORKFLOW="${{ needs.refund-workflow-gate.result }}"
        MULTI_CAMPUS="${{ needs.multi-campus-gate.result }}"
        SESSION_MANAGEMENT="${{ needs.session-management-gate.result }}"
        SKILL_LIFECYCLE="${{ needs.skill-assessment-lifecycle-gate.result }}"
        BOOKING_LIFECYCLE="${{ needs.booking-lifecycle-gate.result }}"
        AUTH_LIFECYCLE="${{ needs.auth-lifecycle-gate.result }}"
        ENROLLMENT_WORKFLOW="${{ needs.enrollment-workflow-gate.result }}"
        USER_ACCOUNT="${{ needs.user-account-gate.result }}"
        INSTRUCTOR_ASSIGNMENT="${{ needs.instructor-assignment-lifecycle-gate.result }}"
        MIGRATION_VOLUME="${{ needs.migration-volume-test.result }}"
        OPERATIONAL_SMOKE="${{ needs.operational-smoke-test.result }}"
        CYPRESS_ADMIN="${{ needs.cypress-web-admin.result }}"
        CYPRESS_INSTRUCTOR="${{ needs.cypress-web-instructor.result }}"
        CYPRESS_STUDENT="${{ needs.cypress-web-student.result }}"
        CYPRESS_BUSINESS="${{ needs.cypress-web-business.result }}"
        CYPRESS_INTEGRATION="${{ needs.cypress-web-integration.result }}"

        if [ "$UNIT" == "success" ] && [ "$SMOKE" == "success" ] && [ "$INTEGRITY" == "success" ] && [ "$ID_GUARD" == "success" ] && [ "$CREDIT_AUDIT" == "success" ] && [ "$SECURITY" == "success" ] && [ "$MIGRATION" == "success" ] && [ "$MIGRATION_VOLUME" == "success" ] && [ "$OPERATIONAL_SMOKE" == "success" ] && [ "$PAYMENT" == "success" ] && [ "$CORE_ACCESS" == "success" ] && [ "$STUDENT_LIFECYCLE" == "success" ] && [ "$INSTRUCTOR_LIFECYCLE" == "success" ] && [ "$REFUND_WORKFLOW" == "success" ] && [ "$MULTI_CAMPUS" == "success" ] && [ "$SESSION_MANAGEMENT" == "success" ] && [ "$SKILL_LIFECYCLE" == "success" ] && [ "$BOOKING_LIFECYCLE" == "success" ] && [ "$AUTH_LIFECYCLE" == "success" ] && [ "$ENROLLMENT_WORKFLOW" == "success" ] && [ "$USER_ACCOUNT" == "success" ] && [ "$INSTRUCTOR_ASSIGNMENT" == "success" ] && [ "$CYPRESS_ADMIN" == "success" ] && [ "$CYPRESS_INSTRUCTOR" == "success" ] && [ "$CYPRESS_STUDENT" == "success" ] && [ "$CYPRESS_BUSINESS" == "success" ] && [ "$CYPRESS_INTEGRATION" == "success" ]; then
          echo "✅ **PASS** - Baseline maintained (100% LIFECYCLE COVERAGE)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Unit & Integration Tests" >> $GITHUB_STEP_SUMMARY
          echo "- Unit tests (full suite, TestDeleteTournament included): 0 failed, 0 errors" >> $GITHUB_STEP_SUMMARY
          echo "- E2E smoke tests: All passing" >> $GITHUB_STEP_SUMMARY
          echo "- API module integrity: All imports clean, route count ≥ 71" >> $GITHUB_STEP_SUMMARY
          echo "- Hardcoded FK ID guard: No hardcoded IDs found (test isolation maintained)" >> $GITHUB_STEP_SUMMARY
          echo "- **Credit balance audit: All credit_balance mutations have CreditTransaction log entries** ✅" >> $GITHUB_STEP_SUMMARY
          echo "- **Security gate: CSRF double-submit (28) + SQL injection (35) = 63 tests PASSED** 🔒" >> $GITHUB_STEP_SUMMARY
          echo "- **Coverage (app/): ≥ 40% enforced** — see \`coverage-report-unit\` artifact for full breakdown" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### E2E Lifecycle Suite (BLOCKING)" >> $GITHUB_STEP_SUMMARY
          echo "- Payment workflow E2E: 3/3 tests passing (0 flake in 20 runs, parallel stable)" >> $GITHUB_STEP_SUMMARY
          echo "- Core Access Sanity: 2/2 tests passing (infra-level coverage)" >> $GITHUB_STEP_SUMMARY
          echo "- **Student Lifecycle E2E: 2/2 tests passing (0 flake in 20 runs, parallel stable)** ✨ NEW" >> $GITHUB_STEP_SUMMARY
          echo "- **Instructor Lifecycle E2E: 1/1 test passing (0 flake in 20 runs, parallel stable)** ✨ NEW" >> $GITHUB_STEP_SUMMARY
          echo "- **Refund Workflow E2E: 1/1 test passing (0 flake in 20 runs, parallel stable)** ✨ NEW" >> $GITHUB_STEP_SUMMARY
          echo "- **Multi-Campus E2E: 1/1 test passing (0 flake in 20 runs, parallel stable)** ✨ NEW" >> $GITHUB_STEP_SUMMARY
          echo "- **Session Management E2E: 4/4 tests passing (0 flake in 20 runs, parallel stable, p95 <200ms)** ✨ NEW" >> $GITHUB_STEP_SUMMARY
          echo "- **Skill Assessment Lifecycle E2E: 9/9 tests passing (0 flake in 20 runs, parallel stable, <5s)** ⚽ NEW - Priority K1" >> $GITHUB_STEP_SUMMARY
          echo "- **Booking Lifecycle E2E: 3/3 tests passing (confirm→waitlist→cancel→promote, capacity guard)** 🎯 Sprint 37" >> $GITHUB_STEP_SUMMARY
          echo "- **Auth Lifecycle E2E: 6/6 tests passing (login→token→refresh→protected endpoint)** 🔐 NEW - Sprint 38" >> $GITHUB_STEP_SUMMARY
          echo "- **Enrollment Workflow E2E: 3/3 tests passing (enroll→auto-approve→duplicate rejected)** 📋 NEW - Sprint 38" >> $GITHUB_STEP_SUMMARY
          echo "- **User Account E2E: 3/3 tests passing (password change → old creds fail → new creds work)** 🔑 NEW - Sprint 39" >> $GITHUB_STEP_SUMMARY
          echo "- **Instructor Assignment Lifecycle E2E: 4/4 tests passing (admin assigns → accept/decline, auth guards)** 👨‍🏫 NEW - Sprint 40" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Cypress Web E2E (Gate C — 5/5 jobs, 339 tests)" >> $GITHUB_STEP_SUMMARY
          echo "- **admin**: all admin CRUD flows, RBAC, responsive layout — ✅" >> $GITHUB_STEP_SUMMARY
          echo "- **instructor**: dashboard, session lifecycle, tournament manage — ✅" >> $GITHUB_STEP_SUMMARY
          echo "- **student**: auth, onboarding, quiz, credits, booking, profile — ✅" >> $GITHUB_STEP_SUMMARY
          echo "- **business-workflow**: booking workflow, session lifecycle workflow — ✅" >> $GITHUB_STEP_SUMMARY
          echo "- **cross-role integration**: full_student_lifecycle.cy.js (15 tests: login→book→start→attend→stop→evaluate) — ✅" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Policy Enforcement" >> $GITHUB_STEP_SUMMARY
          echo "- **0 Flake Tolerance**: All tests must pass 20 sequential runs with 0 failures" >> $GITHUB_STEP_SUMMARY
          echo "- **Parallel Execution**: All tests must pass with \`pytest -n auto\` (race condition validation)" >> $GITHUB_STEP_SUMMARY
          echo "- **Runtime Threshold**: Payment <5s, Lifecycle <30s, Refund <20s" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Safe to merge ✅** - Full lifecycle suite 100% GREEN" >> $GITHUB_STEP_SUMMARY
        else
          echo "❌ **FAIL** - Baseline violated" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Job | Result |" >> $GITHUB_STEP_SUMMARY
          echo "| --- | --- |" >> $GITHUB_STEP_SUMMARY
          echo "| Unit tests | $UNIT |" >> $GITHUB_STEP_SUMMARY
          echo "| E2E smoke tests | $SMOKE |" >> $GITHUB_STEP_SUMMARY
          echo "| API module integrity | $INTEGRITY |" >> $GITHUB_STEP_SUMMARY
          echo "| Hardcoded FK ID guard | $ID_GUARD |" >> $GITHUB_STEP_SUMMARY
          echo "| **Credit balance audit** 💳 | $CREDIT_AUDIT |" >> $GITHUB_STEP_SUMMARY
          echo "| **Security gate (CSRF + SQLi)** 🔒 | $SECURITY |" >> $GITHUB_STEP_SUMMARY
          echo "| **Migration chain integrity** | $MIGRATION |" >> $GITHUB_STEP_SUMMARY
          echo "| **Migration volume safety** | $MIGRATION_VOLUME |" >> $GITHUB_STEP_SUMMARY
          echo "| **Operational smoke test** | $OPERATIONAL_SMOKE |" >> $GITHUB_STEP_SUMMARY
          echo "| Payment workflow E2E | $PAYMENT |" >> $GITHUB_STEP_SUMMARY
          echo "| Core Access E2E | $CORE_ACCESS |" >> $GITHUB_STEP_SUMMARY
          echo "| **Student Lifecycle E2E** | $STUDENT_LIFECYCLE |" >> $GITHUB_STEP_SUMMARY
          echo "| **Instructor Lifecycle E2E** | $INSTRUCTOR_LIFECYCLE |" >> $GITHUB_STEP_SUMMARY
          echo "| **Refund Workflow E2E** | $REFUND_WORKFLOW |" >> $GITHUB_STEP_SUMMARY
          echo "| **Multi-Campus E2E** | $MULTI_CAMPUS |" >> $GITHUB_STEP_SUMMARY
          echo "| **Session Management E2E** | $SESSION_MANAGEMENT |" >> $GITHUB_STEP_SUMMARY
          echo "| **Skill Assessment Lifecycle E2E** ⚽ | $SKILL_LIFECYCLE |" >> $GITHUB_STEP_SUMMARY
          echo "| **Booking Lifecycle E2E** 🎯 | $BOOKING_LIFECYCLE |" >> $GITHUB_STEP_SUMMARY
          echo "| **Auth Lifecycle E2E** 🔐 | $AUTH_LIFECYCLE |" >> $GITHUB_STEP_SUMMARY
          echo "| **Enrollment Workflow E2E** 📋 | $ENROLLMENT_WORKFLOW |" >> $GITHUB_STEP_SUMMARY
          echo "| **User Account E2E** 🔑 | $USER_ACCOUNT |" >> $GITHUB_STEP_SUMMARY
          echo "| **Instructor Assignment Lifecycle E2E** 👨‍🏫 | $INSTRUCTOR_ASSIGNMENT |" >> $GITHUB_STEP_SUMMARY
          echo "| **Cypress — admin** 🌐 | $CYPRESS_ADMIN |" >> $GITHUB_STEP_SUMMARY
          echo "| **Cypress — instructor** 🌐 | $CYPRESS_INSTRUCTOR |" >> $GITHUB_STEP_SUMMARY
          echo "| **Cypress — student** 🌐 | $CYPRESS_STUDENT |" >> $GITHUB_STEP_SUMMARY
          echo "| **Cypress — business-workflow** 🌐 | $CYPRESS_BUSINESS |" >> $GITHUB_STEP_SUMMARY
          echo "| **Cypress — cross-role integration** 🌐 | $CYPRESS_INTEGRATION |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**DO NOT MERGE** - Fix tests first" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Policy**: 0 flake tolerance (20x sequential), parallel execution mandatory" >> $GITHUB_STEP_SUMMARY
          echo "Reference: \`test-infra-stable-baseline\` tag" >> $GITHUB_STEP_SUMMARY
          exit 1
        fi
